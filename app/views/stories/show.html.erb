<h1><%= @story.title %></h1>

<p>
  <%= @story.content %>
</p><br><br>

<%= link_to 'Back', stories_path %><br>
<%= link_to 'Edit', edit_story_path(@story) %><br>
<%= link_to 'Discard', story_path(@story),
                      :data => { :confirm => 'Are you sure?',
                                  :method => 'delete' } %>
